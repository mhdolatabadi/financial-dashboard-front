import { Button, styled } from '@mui/material'

export const ContainedButton = styled(Button)(() => ({
  width: '100%',
  borderRadius: '20px',
  padding: '10px',
  fontSize: '18px',
  margin: '10px 0',
}))
